language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t phileveritt/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true phileveritt/docker-react npm run test -- --coverage